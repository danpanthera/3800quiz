DATABASE_URL="postgresql://postgres:postgres@localhost:15434/quiz3800?schema=public"
JWT_SECRET="change-this-to-a-strong-random-secret-in-production"
JWT_EXPIRES_IN="8h"
PORT=13011

# Chỉ cần khi chạy `npm run giong-doc` để sinh bổ sung audio giọng đọc thuyết
# minh cho câu hỏi riêng của dự án (xem apps/api/scripts/sinh-giong-doc.ts).
GOOGLE_TTS_API_KEY=
